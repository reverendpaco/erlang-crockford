-module(crockford32).

-export([encode/1,
         encode_to_string/1,
         decode/1,
         decode_to_string/1
        ]).

encode(Data) ->
	ok.

decode(Data) ->
    ok.

encode_to_string(Data)->
    ok.

decode_to_string(Data) ->
    ok.
